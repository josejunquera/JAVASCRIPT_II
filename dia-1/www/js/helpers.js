"use strict";

function suma(a, b) {
  return a + b;
}

const today = function () {
  return new Date();
};

const multiply = (a, b) => a * b;

const Pi = 3.14159;

export default suma;
export { today, multiply, Pi };
